import { readFileSync } from "fs"; // lint: unused import

function greet(name: string): string {
  // lint: unused parameter (name is not used in body)
  return "hello";
}

const  unusedVar = 42; // lint: double space after const
