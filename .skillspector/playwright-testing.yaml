# SkillSpector baseline for 'playwright-testing' — accepted HIGH/CRITICAL findings (heuristic false positives).
# Only findings matching BOTH id and path are suppressed; anything new still fails CI.
# Regenerate/review with: skillspector scan skills/.../playwright-testing --no-llm
version: 2
scanner_version: "2.11.2"
rules:
  - id: "MP3"
    path: "SKILL.md"
    reason: "Keyword false positive on test-authoring prose ('reset state'). Reviewed 2026-09-21."
fingerprints: []
